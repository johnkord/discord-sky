apiVersion: apps/v1
kind: Deployment
metadata:
  name: discord-sky-bot
  namespace: discord-sky
  labels:
    app: discord-sky-bot
spec:
  replicas: 1
  strategy:
    type: Recreate
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: discord-sky-bot
  template:
    metadata:
      labels:
        app: discord-sky-bot
    spec:
      containers:
        - name: bot
          image: <ACR_LOGIN_SERVER>/discordskybot:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: discord-sky-secrets
            - configMapRef:
                name: discord-sky-config
            # Generated from ignored --steward-profile files at deploy time. Optional keeps public
            # dark deployments valid without publishing exact guild bindings.
            - configMapRef:
                name: discord-sky-autonomy-bindings
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
          ports:
            - containerPort: 8080
              name: health
          volumeMounts:
            - name: memory-data
              mountPath: /app/data/user_memories
            - name: steward-profiles
              mountPath: /app/steward/profiles
              readOnly: true
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 30
      volumes:
        - name: memory-data
          persistentVolumeClaim:
            claimName: discord-sky-memory
        # Private exact-guild profiles are created out-of-band by deploy.sh. Optional keeps the public
        # dark deployment valid; enabled bindings still fail closed during startup if a key is absent.
        - name: steward-profiles
          secret:
            secretName: discord-sky-steward-profiles
            optional: true
      restartPolicy: Always
